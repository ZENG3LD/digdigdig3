# Environment variables (API keys)
.env
.env.local
.env.*

# Build artifacts
target/
Cargo.lock
*.rlib

# Ralph test artifacts
ralph-test/

# Internal research docs (keep locally, not in repo)
docs/research/
**/research/
ROADMAP.md

# API keys documentation (must not be published)
docs/API-KEYS*
