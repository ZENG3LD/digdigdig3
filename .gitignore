# Environment variables (API keys)
.env
.env.local
.env.*

# Build artifacts
target/
Cargo.lock
*.rlib

# Ralph test artifacts
ralph-test/

# Internal docs — kept locally only, never tracked in git or published.
# Includes architecture notes, research, plans, handoff snapshots, audit
# logs. Treat docs/ as a private workbench.
docs/
ROADMAP.md

# Per-connector research notes (live API exploration before code).
# Already excluded from crates.io tarball via `exclude` in Cargo.toml.
# Untracked here so they don't sit on GitHub either.
**/research/

# Per-connector internal audit / implementation notes (never to GH/crates.io).
**/AUDIT*.md
**/ARCHITECTURE_AUDIT.md
**/IMPLEMENTATION*.md
**/FIX_SUMMARY.md
**/PROBLEM_ANALYSIS.md
**/API_TESTING_REPORT.md
**/AUTHENTICATION_SETUP.md
**/CRYPTO_NO_AUTH.md
**/QUICK_START.md
**/integration_test_guide.md

# deep_smoke + smoke harness output files
deep_smoke_*.txt
smoke_*.txt
*.log

# e2e_smoke / matrix run artefacts (parallel runs, dropped at repo root)
e2e_*.txt
e2e_trace*/
e2e_test_data/
smoke_full_report.txt
catcher_e2e.toml
catcher_e2e.log

# Live capture probe dumps (ad-hoc; live data, not source)
liq_*.log
liq_*.txt
liq_capture/
liq_trace_new/
bybit_liq_*.log
bybit_raw_*.log
bybit_raw_*_stderr.log

# Exchange JSON snapshot dumps (curl probes saved at root during waves)
gateio.json
htx*.json
kucoin*.json
mexc_geo.json
mexc_geo_trace/
upbit_trace*/
okx.json

# Generated screenshots / scratch images (charts/UI investigations)
dash.png
dash_legacy.png

# dig3_storage default persistence root (Phase 1 step 4 output)
dig3_storage/
